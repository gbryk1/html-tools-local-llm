# Claims ledger — claude-code-course.html — intro

Verdicts: ✅ confirmed · ⚠️ imprecise (give fix) · ❌ wrong (give fix) · ❓ unverifiable (soften or remove) · n/a pedagogy/opinion/labelled illustrative

### C0001 · intro · L1 · p
> This is not a list of Claude Code features. The docs already have that, and it changes every week. This guide is about mechanisms: why Claude reads five files before it edits one, why a long session gets worse, why a rule in CLAUDE.md is a request and a hook is a guarantee. When you understand the mechanism, every new feature is just a new knob on a machine you already know.
- pass 1:
- pass 2: n/a pedagogy/framing (CLAUDE.md-vs-hook contrast is covered in its own chapter)

### C0002 · intro · L1 · p
> The path goes from zero to expert in four stages. Chapters 1–3 build the mental model (loop, terminal, context). Chapters 4–8 are the daily-driver skills (memory, permissions, planning, prompting, rewinding). Chapters 9–12 are the extension layer (skills, subagents, hooks, MCP, plugins). Chapters 13–16 are the expert layer (parallel sessions, CI, model choice, habits). Each chapter has one interactive figure: click the buttons, break things, press ↺ Reset.
- pass 1:
- pass 2: n/a course structure/navigation

### C0003 · intro · L1 · p
> Before you start: if you had to design an AI that edits a real repository, what would you worry about most? Wrong edits? Deleted files? Forgetting what it was doing? Running up a bill? Keep your list. By chapter 16 every item on it will have a named feature that exists precisely for it.
- pass 1:
- pass 2: n/a pedagogy

### C0004 · intro · L1 · p
> You do not need to memorize commands. Type / in Claude Code and the full menu appears; press ? on an empty prompt for shortcuts. Memorize the ideas — the menu will remind you of the names. Facts here were checked against the official docs at code.claude.com/docs in September 2026; Claude Code ships often, so run /release-notes when something looks different.
- pass 1:
- pass 2: ✅ commands.md intro — "Type `/` to see the commands available to you"; interactive-mode.md — "`?` on empty input | Toggle the shortcut help panel"; commands.md /release-notes — "View the changelog in an interactive version picker"
